pre-commit:
  parallel: true
  jobs:
    - name: format
      run: pnpm format
      stage_fixed: true

    - name: lint
      run: pnpm lint

pre-push:
  jobs:
    - name: typecheck
      run: pnpm typecheck

    - name: build
      run: pnpm build
