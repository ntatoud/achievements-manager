# Changelog

## 0.1.0 — 2026-02-26

### Features

- Initial version of `achievements` and `achievements-react`
- Add `defineAchievements` utility and `useUnlockedCount` React hook
- Standalone `achievements-react` package with React 19 support
- Use rolldown for library builds
